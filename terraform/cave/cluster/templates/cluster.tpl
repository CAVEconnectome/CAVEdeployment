cluster:
  globalServer: ""
  environment: "${environment}"
  domainName: "${domain_name}"
  googleProject: "${project_id}"
  dataProjectName: "${data_project_id}"
  meshPool: "${mesh_pool}"
  standardPool: "${standard_pool}"
  lightweightPool: "${lightweight_pool}"
  corePool: "${core_pool}"
  